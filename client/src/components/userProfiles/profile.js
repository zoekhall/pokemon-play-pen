import React from 'react';
import InfoSect from './infoSect.js';

const Profile = (props) => {
  return (
    <div>
      <InfoSect id={props.id}/>
      profiless
    </div>
  );
};

export default Profile;
